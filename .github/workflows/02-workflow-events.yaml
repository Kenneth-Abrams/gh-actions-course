name: 02 - Building Blocks

on:
  push:
  workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
        - name: Show the trigger
          run: |
            echo "EVent name: $ {{ github.event_name }}"
